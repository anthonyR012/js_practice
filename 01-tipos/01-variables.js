let nombre = "Alberto"; // Declaración de una variable de tipo string
var edad = 30; // Declaración de una variable de tipo number
/*La principal diferencia entre let y var en JavaScript radica
 en su alcance (scope).
 var tiene un alcance de función, mientras que let tiene un alcance de bloque.
 Esto significa que las variables declaradas con var son accesibles dentro de toda la función 
 en la que se definen, independientemente de si están dentro de un bloque if, for, 
 o cualquier otro bloque. En cambio, las variables declaradas 
 con let solo son accesibles dentro del bloque específico (entre llaves {}) 
 donde se declararon.
*/
console.log(nombre);


